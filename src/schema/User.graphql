enum Role {
  ADMIN
  CLIENT
}

type User {
  id: ID!
  name: String!
  email: String!
  picture: String
}

type Query {
  allUsers: [User!]!
}

type Mutation {
  signInGoogle(token: String!): SigninPayload!
}

type SigninPayload {
  token: String
  user: User
}
